<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :night_hours %><br />
    <%= f.number_field :night_hours %>
  </div>
  <div class="field">
    <%= f.label :day_hours %><br />
    <%= f.number_field :day_hours %>
  </div>
  <div class="field">
    <%= f.label :total_hours %><br />
    <%= f.number_field :total_hours %>
  </div>
  <div class="field">
    <%= f.label :total_salary %><br />
    <%= f.number_field :total_salary %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
